#
# Tests Directory
#

# List files to compile/test
add_catch_test(aligned)
add_catch_test(allocator)
add_catch_test(aligned_allocator)
add_catch_test(intrusive_base)
add_catch_test(intrusive_derived)
add_catch_test(intrusive_ptr)

